<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:cu="clr-namespace:ManageBluetooth.Custom"
    xmlns:const="clr-namespace:ManageBluetooth.Models.Constants"
    xmlns:cv="clr-namespace:ManageBluetooth.Converters"
    xmlns:res="clr-namespace:ManageBluetooth.Resources"
    xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
    x:Class="ManageBluetooth.Views.BluetoothPage">

    <ContentPage.Resources>
        <ResourceDictionary>
        </ResourceDictionary>
    </ContentPage.Resources>

    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <cu:RoundedCustomFrame
                Grid.Row="0"
                CornerRadius="0, 0, 20, 20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <Label
                        Text="{Binding BluetoothState.Localized}"
                        FontSize="Body"
                        Grid.Column="0">
                        <Label.Style>
                            <Style
                                TargetType="Label">
                                <Style.Triggers>
                                    <DataTrigger
                                        TargetType="Label"
                                        Binding="{Binding IsBluetoothEnabled}"
                                        Value="True">
                                        <Setter
                                            Property="TextColor"
                                            Value="White" />
                                    </DataTrigger>
                                    <DataTrigger
                                        TargetType="Label"
                                        Binding="{Binding IsBluetoothEnabled}"
                                        Value="False">
                                        <Setter
                                            Property="TextColor"
                                            Value="Black" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Label.Style>
                    </Label>
                    <Switch
                        IsToggled="{Binding IsBluetoothEnabled}"
                        OnColor="#F6F6F6"
                        ThumbColor="White"
                        xct:TouchEffect.Command="{Binding ChangeBluetoothStatusCommand}"
                        HorizontalOptions="End"
                        Grid.Column="1">
                    </Switch>
                </Grid>
                <cu:RoundedCustomFrame.Style>
                    <Style
                        TargetType="cu:RoundedCustomFrame">
                        <Style.Triggers>
                            <DataTrigger
                                TargetType="cu:RoundedCustomFrame"
                                Binding="{Binding IsBluetoothEnabled}"
                                Value="True">
                                <Setter
                                    Property="BackgroundColor"
                                    Value="{x:Static const:ColorValueConstants.DarkBlueColor}" />
                            </DataTrigger>
                            <DataTrigger
                                TargetType="cu:RoundedCustomFrame"
                                Binding="{Binding IsBluetoothEnabled}"
                                Value="False">
                                <Setter
                                    Property="BackgroundColor"
                                    Value="White" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </cu:RoundedCustomFrame.Style>
            </cu:RoundedCustomFrame>

            <ContentView
                Grid.Row="1">
                <Grid>
                    <ScrollView>
                        <StackLayout>
                            <Label
                                Text="{Binding IsBluetoothEnabledMessage.Localized}"
                                FontSize="Body"
                                Padding="10" />
                            <StackLayout
                                IsVisible="{Binding IsBluetoothEnabled}"
                                BindableLayout.ItemsSource="{Binding ConnectedDevicesList}">
                                <BindableLayout.ItemTemplate>
                                    <DataTemplate>
                                        <StackLayout>
                                            <Label
                                                Text="{Binding .}" />
                                        </StackLayout>
                                    </DataTemplate>
                                </BindableLayout.ItemTemplate>
                            </StackLayout>

                        </StackLayout>
                    </ScrollView>
                </Grid>
            </ContentView>



        </Grid>
    </ContentPage.Content>
</ContentPage>